{% if entity.contact is defined and entity.contact is not empty %}{{ entity.contact.fullName }}{% elseif entity.firstName is defined and entity.firstName is not empty %}{{ entity.firstName }}{% endif %}
<div class="sp-block-outer sp-block-outer-{{ data.block.type }} section register-forms {{ data.block.class }} {% if is_granted('ROLE_ADMIN') %}is_admin{% endif %}" {{ data.block.dataAttributes }}>
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-6 col-md-offset-3 text-center">
            {{ render(controller('ScommerceBusinessBundle:Template:twigIncludeTwig', {'template':'Components/Form:reset_password_form.html.twig', 'data': {'triggerAction': false}}, {})) }}
        </div>
    </div>
</div>

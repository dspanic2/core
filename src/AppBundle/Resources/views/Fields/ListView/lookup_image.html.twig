<td {% if fieldClass is not empty %}class="{{ fieldClass }}"{% endif %}>
    <div class="sp-document-list-wrapper" {% if width is not empty %}style="width: {{ width }}px;"{% endif %}>
        <img src="{{ attribute.folder }}{{ value }}"/>
    </div>
</td>
